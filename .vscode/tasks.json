{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc -g -Wall -std=c99 -D WIN -o hsolver.exe hsolver.c",
            "problemMatcher": "$gcc",
            "presentation": {
                "reveal": "silent",
            }
        }
    ]
}